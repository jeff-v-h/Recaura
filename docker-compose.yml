version: '3.4'  
  
services:
  api:
    build:  
      context: ./api 
      dockerfile: Dockerfile
    ports:
      - '5555:5555'
    expose:
      - 5555
    depends_on:
      - sqlserver
  sqlserver:
    build:
      context: ./db
      dockerfile: Dockerfile
    environment:  
      ACCEPT_EULA: Y  
      SA_PASSWORD: "recApp123" 
    ports:  
      - '1433:1433'  
    expose:  
      - 1433